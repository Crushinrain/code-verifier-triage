# Task Ticket

```yaml
task_id: T000
title: Initialize repository, governance control plane, and minimal structure
owner_role: Planner
executor_role: Executor
status: HANDED_BACK_FOR_REVIEW
objective: >-
  Create an independent, governed, reproducible bootstrap repository from the
  verified starter pack and bind the work in one T000 root commit.
inputs:
  - E:/code_verifier_triage_bundle (read-only source; local validator PASS)
  - tasks/task_graph.yaml#T000
  - governing-project-workflows skill
allowed_paths:
  - /data3/xc/code-verifier-triage/**
  - /data3/xc/.agents/skills/governing-project-workflows/**
forbidden_paths:
  - /data3/xc/.git/**
  - /data3/xc/PV_forecast/**
  - /data3/xc/<all-other-sibling-projects>/**
  - approvals/**
  - contracts/**
  - data/final_test/**
depends_on: []
commands:
  - sha256sum -c MANIFEST.sha256
  - /data3/xc/.conda/envs/d2l/bin/python scripts/validate_bundle.py
  - python3 <workflow-skill>/scripts/workflow.py init <project-root>
  - python3 <workflow-skill>/scripts/workflow.py inspect <project-root> --json
  - python3 <workflow-skill>/scripts/workflow.py validate <project-root> --json
  - git init -b main
  - git diff --check
  - git commit -m "chore: bootstrap CodeVerifier-Triage T000"
outputs:
  - .workflow/project.json
  - PLAN.md
  - HANDOFF.md
  - PROGRESS.md
  - REVIEW_PROTOCOL.md
  - pyproject.toml
  - THIRD_PARTY_NOTICES.md
  - LICENSE_AUDIT.md
  - tasks/tickets/T000.md
  - docs/governance/T000_GOVERNANCE_GAPS.md
acceptance:
  - bundle validator passes
  - one Task ID maps to one root commit
  - generated and heavy artifacts are not tracked
  - parent repository control files and PV_forecast remain untouched
  - main branch protection is configured and independently verified
stop_conditions:
  - source or remote manifest mismatch
  - existing target repository or same-name skill path
  - required write outside authorized paths
  - request to modify a frozen contract, approval, final data, or sibling project
  - request to install dependencies, use GPU, execute candidate code, or start T001
human_approval_required: false
approval_key: null
reviewer_role: Fresh role-locked Reviewer
```

## Context

The source pack is PLANNED and contains no project experiment results. The Human
Owner explicitly authorized the bootstrap actions. No remote URL was provided,
so hosted branch protection remains an observable failed acceptance item rather
than an inferred success.

## Non-goals

- No T001 inventory, T002 upstream clone, T003 completed legal clearance, T004
  environment creation, or T005 CI/pre-commit implementation.
- No changes to frozen contracts, approvals, task graph, or planning Claims.
- No model/data access, GPU work, candidate execution, Docker changes, final
  access, deletion, or release.

## Rollback

The root commit is the reproducible boundary. A Reviewer/Human Owner may archive
the ledger and revert/remove the isolated target only through a separately
authorized destructive action. The Executor performs no rollback automatically.
